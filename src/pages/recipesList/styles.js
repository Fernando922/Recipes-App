import styled from 'styled-components/native';
import { WHITE } from '../../utils/colors';

export const Container = styled.View`
  flex: 1;
`;

export const NoRecipes = styled.Text`
  font-size: 16px;
  text-align: center;
  color: ${WHITE};
  margin-top: 20px;
`;
